const styles = () => ({
    AddEntryButton: {
        display: "flex",
        justifyContent: "flex-end"
    },
    button:{
        marginRight:10,
        marginBottom:10
    }
});

export default styles;
